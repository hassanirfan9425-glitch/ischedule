// Single source of truth for the version shown in-app (nav drawer, More pages, Settings, login
// screen). Bump alongside client/package.json, server/package.json, and android/app/build.gradle's
// versionName/versionCode when cutting a new release.
export const APP_VERSION = '1.0.0';
