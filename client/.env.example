# Production only — the exact URL of your deployed Render backend, including /api, no trailing slash.
# Leave unset for local dev (Vite's dev-server proxy handles /api automatically).
VITE_API_URL=https://your-api-name.onrender.com/api
